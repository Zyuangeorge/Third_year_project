class utilFunctions():

    def returnIndex(item, itemList):
        """Return the index of the item in the list based on the item"""
        item = item
        list = itemList

        for n in itemList:
            if n = item:
                return n.index()

